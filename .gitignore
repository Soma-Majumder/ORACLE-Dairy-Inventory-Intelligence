.claude/settings.local.json
node_modules/
.DS_Store

# API keys are never stored in files — the agent reads a bring-your-own key
# from the browser's localStorage. These lines are just a safety net in case
# tooling is added later.
.env
.env.*
*.local
